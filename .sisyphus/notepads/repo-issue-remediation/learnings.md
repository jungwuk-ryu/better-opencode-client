# Learnings

- 2026-03-29: Aligned the Dart package identity by changing `pubspec.yaml` `name:` to `better_opencode_client` and updating all `package:opencode_mobile_remote/...` imports in the allowed Dart groups: `lib/`, `test/`, `tool/`, and `integration_test/`.
- 2026-03-29: The package rename was purely a package-prefix swap; native bundle IDs and Android signing settings were intentionally left untouched, so the safe pattern was to replace only the import prefix and avoid any platform config edits.
- 2026-03-29: Shared request URI construction should normalize the base path to a trailing slash, strip any leading slash from the request path before `resolve`, and merge queries as `baseUri.queryParameters` followed by request-specific keys so `/api` prefixes and base tokens are preserved while request keys override duplicates.
- 2026-03-29: Migrated the scoped project, chat, file, request, settings, and SSE services to , and hardened their task-scoped tests so  prefixes and base  query params must survive every migrated request path.
- 2026-03-29: Migrated the scoped project, chat, file, request, settings, and SSE services to buildRequestUri, and hardened their task-scoped tests so /api prefixes and base token query params must survive every migrated request path.
- 2026-03-29: Remote `session.deleted` handling in `WorkspaceController` now removes the full deleted session tree (root + descendants), reuses local-delete fallback semantics for selected-session removal, clears per-session live state (watched timelines, queued prompts, todo cache, preview caches, pending requests, notifications), and clears message/spill cache keys for every removed session ID.
- 2026-03-29: Added live-sync regression coverage for remote selected-tree deletion fallback, non-selected deletion cleanup isolation, last-session remote deletion empty-state safety, and already-empty workspace deletion safety; the scoped test suite passes with `/home/ubuntu/tools/flutter/bin/flutter test test/features/web_parity/workspace_controller_live_sync_test.dart`.
- 2026-03-29: Workspace web-parity SSE recovery now uses one shared dropped-stream path for both `onDone` and `onError` (`cancelOnError: true`), guards against parallel recovery loops, disconnects stale subscriptions before reconnecting, reloads the current workspace/session bundle, and resumes live message/todo/permission event handling without duplicate event application.
- 2026-03-29: Request malformed-payload hardening needs validation at both boundaries: `request_event_applier.dart`/`request_service.dart` must skip malformed or partial `question.asked`/`permission.asked` payloads and pending items without changing prior state, and the adjacent `WorkspaceController` auto-accept decode for live `permission.asked` must use the same safe-return guard so a bad event cannot block later valid ones.
- 2026-03-29: Cleaned l10n ARB duplicates (`shellRenameSessionTitle`, `shellSessionTitleHint`, `shellCancelAction`, `shellSaveAction`) and normalized a few obvious Korean UI labels ("Thinking" -> "사고", "diff" -> "변경 사항", "todo" -> "할 일", plus "shell/attach" -> "셸/연결"), then regenerated `app_localizations_ko.dart` via `flutter gen-l10n` and confirmed the scoped copy accessibility tests still pass.
- 2026-03-29: Repo hygiene for Apple workspaces should ignore `**/xcshareddata/IDEWorkspaceChecks.plist` and keep those Xcode-generated files out of version control; the tracked iOS/macOS workspace-check plists were removable noise with no functional project configuration.
- 2026-03-29: Xcode workspace-check noise was tracked in both top-level `Runner.xcworkspace` directories and nested `.xcodeproj/project.xcworkspace` directories, so the reliable acceptance check is `git ls-files | grep "IDEWorkspaceChecks.plist"` rather than assuming only the top-level workspace files exist.
- 2026-03-29: Refreshed repo-facing docs for Task 9 by replacing the template README with project-specific setup/run/test/debug guidance, adding explicit manual-tool references under `tool/manual/`, and summarizing the post-fix URI, recovery, and malformed-payload resilience behavior.
- 2026-03-29: GitHub Actions cannot assume a local `/home/ubuntu/tools/flutter` install; the CI workflow now provisions Flutter on the runner (via `subosito/flutter-action`) and then symlinks the installed SDK into `/home/ubuntu/tools/flutter` so the repo’s absolute-path flutter command convention stays intact.
- 2026-03-29: Manual tool scripts under `tool/manual/` are most reliably invoked via `dart run tool/manual/<script>.dart` (after deps are fetched) rather than `flutter run`, since these scripts are plain Dart entrypoints.
- 2026-03-29: Task 7 follow-up: removed remaining mixed-English Korean user-facing l10n in the connection/diagnostics area (`staleStream`, `resyncStream`, `capabilityFlags`, `connectionCapabilitiesLabel`, `connectionOutcomeUnsupported`, `capabilityHasProviderOAuth`, plus adjacent `spec fetch`/`OpenAPI spec` wording), regenerated `app_localizations_ko.dart`, and re-ran the home/shell copy accessibility tests.
