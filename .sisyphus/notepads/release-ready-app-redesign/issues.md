- 2026-03-19: Widget-test compilation was blocked because cache-settings localization getters were referenced in `lib/src/` but missing from both `.arb` files; the task was unblocked by adding the missing localization keys and regenerating `lib/l10n/app_localizations.dart`.
- 2026-03-19: Broad `flutter test` currently fails in `test/features/shell/opencode_shell_screen_test.dart` with multiple `pumpAndSettle timed out` failures tied to the existing shell screen, so Task 2 should avoid assuming the full suite is green before secure-storage work starts.
- 2026-03-19: Secure-profile migration needs defensive parsing on both legacy prefs JSON and secure-store JSON; malformed or partial payloads should be dropped or scrubbed instead of throwing during load or draft restore.
- 2026-03-19: Task 3 still cannot distinguish "resume data unknown" from "no last session exists" because `ProjectTarget.lastSession` is either populated or null; the new launch model currently treats null or empty hints as `sessionMissing` until the upstream project/session API exposes that difference.
- 2026-03-19: Task 4 keeps saved-server presentation deliberately simple (`_ServerTile` + launch-status pill) and routes all setup/detail work back through `ConnectionHomeScreen`; Task 5 should replace that stopgap with the intended server-card design instead of layering more visuals onto the temporary list treatment.
- 2026-03-19: In the `flutter run -d web-server` screenshot path, the browser loaded DDC scripts but never painted a Flutter first frame, leaving raw `web/index.html` as a white page; Task 4 now includes a branded bootstrap shell in `web/index.html` that hides on the `flutter-first-frame` event so the runtime gate still shows the workspace home instead of a blank screen.
- 2026-03-19: `flutter test integration_test/workspace_resume_flow_test.dart` on this headless Linux container built the app but failed before Dart ran because the GTK runner exited immediately with `cannot open display`; Task 7's verification now depends on the Linux runner providing a headless-display fallback when `DISPLAY` is unset.
- 2026-03-19: Task 9 intentionally removes terminal, raw inspector, config preview, and integration status from the primary `Settings` destination; Task 10 still needs to decide where those advanced surfaces return once Advanced/settings segregation is implemented.
- 2026-03-19: Task 10 integration-path verification cannot safely advertise `/event` against the mock `127.0.0.1:8787` profile, because `_connectEvents()` performs a real stream attempt and can fail the test with socket exceptions before Advanced-panel assertions run.
- 2026-03-20: `"/home/ubuntu/tools/flutter/bin/flutter" test integration_test` still fails on the Linux desktop runner after the first file with `Error waiting for a debug connection: The log reader stopped unexpectedly, or never started.`; this reproduces even with only pre-existing files (`project_open_flow_test.dart` followed by `workspace_resume_flow_test.dart`), so the remaining blocker is a broader multi-file desktop integration restart issue rather than the new release regressions.
- 2026-03-20: After the desktop restart blocker was removed, `integration_test/advanced_tools_flow_test.dart` still failed in the full suite because it asserted the localized `Open Advanced` label instead of the stable `settings-open-advanced` key; using the key made the test resilient to the compact settings presentation that still exposes the same action.
- 2026-03-20: Repo-local investigation for the Linux multi-file restart failure is exhausted: this repo has no `flutter_test_config.dart`, no integration hook that can replace desktop process launch/log discovery, and no repo-local override for `DesktopLogReader`; with only repo-local changes, `flutter test integration_test` still fails after the first file because Flutter's external `packages/flutter_tools/lib/src/desktop_device.dart` closes the shared desktop log stream after one launch, matching upstream issue `flutter/flutter#135673`.
