- 2026-03-19: Characterization coverage stays tied to the current shell branch thresholds (`700`, `960`, `1320`) so later redesign work can measure deliberate breakpoint changes instead of absorbing accidental UI shifts.
- 2026-03-19: Persistence characterization asserts raw `SharedPreferences` storage, not just round-trips, so the secure-storage migration has an explicit before-state for saved and draft credentials.
- 2026-03-19: Secure credentials are keyed separately from `SharedPreferences` payloads: saved profiles use a secure key derived from profile id, drafts use a fixed draft key, and migration rewrites legacy plaintext JSON only after the secure write succeeds so repeated loads remain idempotent and `ServerProfile.storageKey` semantics stay unchanged.
- 2026-03-19: The release home state model should live outside `core/connection` and map from `ServerProbeReport` plus existing project/session hints, so probe-path terms stay contained in infrastructure and do not become the public launch vocabulary.
- 2026-03-19: `LaunchHomeState` splits launch concerns into `LaunchServerState` and `LaunchRoutingState`, while `mapLaunchConnectionStatus()` is the only new bridge from `ServerProbeReport` classifications into the product-facing status vocabulary (`ready`, `signInRequired`, `offline`, `incompatible`).
- 2026-03-19: The new workspace home stays intentionally thin: it owns branded launch scaffolding, saved-server selection, cached launch-status display, and routing into the existing `ConnectionHomeScreen` seam, while real server-card redesign and richer connect/resume behaviors remain separate follow-on tasks.
- 2026-03-19: Task 5 keeps `WorkspaceHomeScreen` as the primary release surface and routes add/edit into `ConnectionHomeScreen` through explicit entry parameters instead of duplicating editor persistence on home or sending users back to a generic probe-first landing screen.
- 2026-03-19: Task 6 keeps `ConnectionHomeScreen` as the edit/details seam only; `WorkspaceHomeScreen` owns the connect-and-continue state machine, while the existing `OpenCodeServerProbe` plus the same cache/recent persistence paths remain the single compatibility/auth source of truth.
- 2026-03-19: Task 8 keeps project discovery in the home-owned ready flow by passing `ProjectCatalogService`, `ProjectStore`, and `StaleCacheStore` into `ProjectWorkspaceSection` from `WorkspaceHomeScreen` instead of letting the connection/details screen act as a parallel launcher.
- 2026-03-19: When server project data is missing or temporarily unavailable, the chooser should degrade to friendly recovery options (`recent projects` and `folder path`) rather than hiding the surface or showing raw transport errors.
- 2026-03-19: Task 9 keeps `Settings` product-facing and intentionally narrow: it exposes only cache settings as a stable primary destination while terminal, raw inspector, config preview, and integration status stay deferred for the later Advanced/settings split.
- 2026-03-19: The touched session-selection regression test now proves the explicit compact `Sessions -> session -> Chat` path instead of relying on a brittle desktop text hit target, because Task 9's contract is stable session access across breakpoints rather than a specific wide-layout tap seam.
- 2026-03-19: Task 10 verification should keep `integration_test/advanced_tools_flow_test.dart` network-safe by avoiding `/event` capability simulation there; event-stream probes can trigger real socket connection attempts during integration binding and destabilize UI-path assertions unrelated to event transport.

- 2026-03-20: Release copy avoids internal vocabulary in default strings: replace `probe` with `server check`, `experimental` with `advanced`, remove phase numbering, and rename `project preview` to `project details` (EN/KO parity) while keeping localization keys stable.
